interface:
  display_name: "Chat"
  short_description: "Repo-specific patterns and workflows for chat"
  default_prompt: "Use the chat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true